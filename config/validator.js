'use strict'

module.exports = {
  // 背景、滑动图片长宽参数
  canvasWidth: 326,
  canvasHeight: 160,
  sliceWidth: 50,
  sliceHeight: 50,

  // 后台滑动验证精度(px)
  accuracy: 3,
  // 前台验证框底部提示预留高度(px)
  tipHeight: 20
}
